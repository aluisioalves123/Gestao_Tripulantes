<p id="notice"><%= notice %></p>

<%= image_tag(@user.avatar.thumb) if @user.avatar %>

<p>
  <strong>Admin:</strong>
  <%= @user.admin %>
</p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<strong>Projetos participados</strong>
<br>
<% @project_users.each do |project_user| %>
  <%= project_user.project.nome %>

<br><br>
<% end %>

<div class="action-item"><a><h5><%= link_to 'Editar', edit_user_path(@user) %></h5></a></div>
<div class="action-item"><a><h5><%= link_to 'Back', users_path %></h5></a></div>
